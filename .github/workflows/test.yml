name: Test
on: push

jobs:
  container-job:
    runs-on: windows-latest
    steps:
      - name: Pull some image
        run: |
          docker pull mcr.microsoft.com/windows/servercore:20H2
          docker run mcr.microsoft.com/windows/servercore:20H2 echo "Hello World!"
