name: Test
on: push

jobs:
  container-job:
    runs-on: windows-latest
    steps:
      - name: Pull some image
        run: |
          docker pull mcr.microsoft.com/windows/servercore:ltsc2019
          docker run mcr.microsoft.com/windows/servercore:ltsc2019 echo "Hello World!"
