name: Test
on: push

jobs:
  container-job:
    runs-on: windows-latest
    steps:
      - name: Pull some image
        run: |
          docker pull microsoft/windowsservercore
          docker run microsoft/windowsservercore echo "Hello World!"
